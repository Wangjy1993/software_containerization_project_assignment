apiVersion: v1
kind: Service
metadata:
  name: {{ include "sc-chart.fullname" . }}
  labels:
    {{- include "sc-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "sc-chart.selectorLabels" . | nindent 4 }}
