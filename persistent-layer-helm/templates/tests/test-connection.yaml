apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "persistent-layer-helm.fullname" . }}-test-connection"
  labels:
    {{- include "persistent-layer-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "persistent-layer-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
